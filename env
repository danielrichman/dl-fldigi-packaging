#!/bin/bash

SRC=$PREFIX/src
HTGT="i586-mingw32msvc"
CONFLAGS="--build=i686-pc-linux-gnu --host=$HTGT --enable-static --disable-shared"

let MAKE_CORES=`cat /proc/cpuinfo | grep processor | wc -l`
if [ $MAKE_CORES -lt 1 ]; then
	MAKE_CORES=1
fi

export SRC HTGT CONFLAGS MAKE_CORES
